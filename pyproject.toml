[project]
name = "changneung-change-poc"
version = "0.1.0"
description = "Goyang Changneung Building Change Intelligence PoC - public-data baseline for a reusable change-detection pipeline"
requires-python = ">=3.12"
readme = "README.md"

[tool.setuptools.packages.find]
where = ["src"]
